apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chart-teste.fullname" . }}-test-connection"
  labels:
    {{- include "chart-teste.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chart-teste.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
