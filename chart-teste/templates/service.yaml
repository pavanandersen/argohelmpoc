apiVersion: v1
kind: Service
metadata:
  name: {{ include "chart-teste.fullname" . }}
  labels:
    {{- include "chart-teste.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chart-teste.selectorLabels" . | nindent 4 }}
